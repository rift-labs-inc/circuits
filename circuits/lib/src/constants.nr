global MAX_ENCODED_CHUNKS: u32 = 226; // ~7 kb max txn size
global BYTES_PER_CHUNK: u32 = 31;
global MAX_TXN_BYTES: u32 = MAX_ENCODED_CHUNKS*BYTES_PER_CHUNK;
global MAX_LIQUIDITY_PROVIDERS: u32 = 175;
